import math
class Solution(object):
    def mySqrt(self, x):
        """
        :type x: int
        :rtype: int
        """
        sqrt_x=math.sqrt(x)
        sqrt_x=int(sqrt_x)

        return sqrt_x